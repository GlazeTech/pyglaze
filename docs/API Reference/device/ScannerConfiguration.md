# ScannerConfiguration

**`pyglaze.device.ScannerConfiguration`**

::: pyglaze.device.ScannerConfiguration

## Examples


#### Make a scan configuration

=== "Make a scan configuration"

    ```py
    from pyglaze.device import ScannerConfiguration

    config = ScannerConfiguration(use_ema=True)
    ```
